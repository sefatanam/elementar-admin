import { ComparisonSliderBeforeImageDirective } from './comparison-slider-before-image.directive';

describe('ComparisonSliderBeforeImageDirective', () => {
  it('should create an instance', () => {
    const directive = new ComparisonSliderBeforeImageDirective();
    expect(directive).toBeTruthy();
  });
});
