import { NotificationActorAvatarDirective } from './notification-actor-avatar.directive';

describe('NotificationActorAvatarDirective', () => {
  it('should create an instance', () => {
    const directive = new NotificationActorAvatarDirective();
    expect(directive).toBeTruthy();
  });
});
