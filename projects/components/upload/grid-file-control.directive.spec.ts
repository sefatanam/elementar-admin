import { GridFileControlDirective } from './grid-file-control.directive';

describe('GridFileControlDirective', () => {
  it('should create an instance', () => {
    const directive = new GridFileControlDirective();
    expect(directive).toBeTruthy();
  });
});
