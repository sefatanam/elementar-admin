import { UploadAreaIconDirective } from './upload-area-icon.directive';

describe('UploadAreaIconDirective', () => {
  it('should create an instance', () => {
    const directive = new UploadAreaIconDirective();
    expect(directive).toBeTruthy();
  });
});
